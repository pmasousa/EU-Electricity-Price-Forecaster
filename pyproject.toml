[project]
name = "electricity-price-forecaster"
version = "0.2.0"
description = "A multi-country day-ahead electricity spot price forecasting model (Temporal Fusion Transformer)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "pandas",
    "numpy",
    "darts[torch]==0.46.1",
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "openmeteo-requests",
    "requests-cache",
    "retry-requests",
    "matplotlib",
    "scikit-learn",
    "mlflow",
    "gradio",
    "numba>=0.58.0",
    "lightgbm"
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "ruff>=0.3.0",
    "jupyter>=1.0.0",
    "pyright>=1.1.411",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "N", "B"]
